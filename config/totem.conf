CONFIG_ZMK_USB_LOGGING=n

# Mouse / pointing support (needed for the FN layer mouse keys: &mmv &msc &mkp)
CONFIG_ZMK_POINTING=y
# Mouse speed is set in totem.keymap via #define ZMK_POINTING_DEFAULT_MOVE_VAL (not here)
